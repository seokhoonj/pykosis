[build-system]
# Cap <1.32: hatchling 1.32 emits Metadata-Version 2.5, which the twine bundled in the pinned
# PyPI-publish action rejects (pypa/twine#1327). 2.4 is accepted by PyPI and that twine; the
# 1.27 floor is PEP 639 (the SPDX license expression and license-files). Lift once twine updates.
requires = ["hatchling>=1.27,<1.32"]
build-backend = "hatchling.build"

[project]
name = "pykosis"
version = "0.1.0"
description = "Python client for the KOSIS (Korean Statistical Information Service) Open API"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Seokhoon Joo", email = "seokhoonj@gmail.com" }]
keywords = ["kosis", "statistics korea", "korea", "official statistics", "data"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Information Analysis",
]
dependencies = ["httpx>=0.27"]

[project.optional-dependencies]
dev = [
    "mypy>=1.11",
    "pytest>=8.0",
    "ruff>=0.6",
]

[project.scripts]
kosis = "pykosis.cli:main"

[project.urls]
Homepage = "https://github.com/seokhoonj/pykosis"
Source = "https://github.com/seokhoonj/pykosis"
Issues = "https://github.com/seokhoonj/pykosis/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/pykosis"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.per-file-ignores]
# Generated: a few Korean indicator names push the spec line past the limit.
"src/pykosis/curation/_generated.py" = ["E501"]

[tool.mypy]
files = ["src"]
python_version = "3.11"
strict = true
